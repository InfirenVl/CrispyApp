<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Разрешение на использование интернета -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Разрешение на доступ к состоянию сети -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_crispy"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_crispy_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CrispyApp"
        tools:targetApi="31">
        <activity
            android:name=".view.activity.BoardActivity"
            android:exported="false" />
        <activity
            android:name=".view.activity.WorkspaceActivity"
            android:exported="false" />
        <activity
            android:name=".view.activity.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".view.activity.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".view.activity.RegistrationActivity"
            android:exported="false" />
        <activity
            android:name=".view.activity.HelpActivity"
            android:exported="false" />
        <activity
            android:name=".view.activity.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activity.MainActivity"
            android:exported="false" />
    </application>

</manifest>